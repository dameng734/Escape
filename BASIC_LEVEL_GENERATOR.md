# 独立随机纯基础块关卡生成器

关卡 1–3 保留教学内容。关卡 4–180 使用 8×8 棋盘，只允许目标块、八类普通基础块、固定块和出口。

每一关都从新的随机种子独立生成完整拓扑，禁止母版复制、镜像、旋转、模板替换或从少量棋盘批量扩展。

最佳步数安排：

- 第 4–6 关：12 步。
- 此后每 3 关增加 1 步。
- 达到 30 步后保持 30 步，最高不超过 30 步。

最佳步数采用与游戏完全一致的口径：目标块必须在出口方向继续移动一步、实际离开棋盘后才算通关。生成器不会把构造关卡时的移动次数当作最佳步数。

星级阈值固定为：三星不超过最佳步数，二星不超过最佳步数加 2，关卡最大允许步数为最佳步数加 5。禁止按关卡深度使用百分比放大阈值。

每个候选关卡都执行以下精确流程：

1. 随机生成一个完整棋盘拓扑。
2. 完整枚举该拓扑的可达连通状态空间。
3. 以所有出口就绪状态作为多源最短路起点。
4. 对完整状态空间执行分层广度优先搜索。
5. 只接受真实最短距离恰好位于指定深度的状态。
6. 重放包含最终离开棋盘动作的完整解。
7. 在独立验证阶段再次完整枚举并核对最短距离。

硬性限制：

- 空格 5–8 格。
- 固定块不超过 6 块；可以为 0。
- 八类普通块全部出现，最多类型数量与最少类型数量之差不超过 1。
- 禁止磁铁、冰块、金币、传送门、锁和钥匙。
- 跨关比较归一化最优解三步片段、目标块路线和完整拓扑签名。
- 解法片段 Jaccard 相似度超过 58% 的候选直接淘汰并重新随机。
- 不使用对称扩展，不使用模板扩展。

构建：

```bat
tools\basic_generator\build.cmd
```

完整生成：

```bat
tools\basic_generator\independent_generator.exe --from 4 --to 180 --seed 202606201530 --topology-attempts 50000 --max-component-states 2000000
```

独立验证：

```bat
tools\basic_generator\independent_generator.exe --from 4 --to 180 --validate-only --max-component-states 2000000
```

生成器每完成一关就原子写入断点文件。任何状态空间未完整枚举、最短距离不匹配、空格或块类型不合规、解法重放失败、或跨关相似度超限，都会直接判定失败，不会降低标准。
